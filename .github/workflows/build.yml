
name: Build Meteor Addon

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'microsoft'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Initialize Project Structure
        run: |
          mkdir -p src/main/java/com/example/addon
          mv AddonTemplate.java src/main/java/com/example/addon/
          
          cat << 'EOF' > build.gradle
          plugins {
              id 'fabric-loom' version '1.7-SNAPSHOT'
              id 'maven-publish'
          }
          version = '1.0.0'
          group = 'com.example'
          repositories {
              maven { url 'https://meteordev.org' }
              maven { url 'https://meteordev.org' }
              mavenCentral()
          }
          dependencies {
              minecraft "com.mojang:minecraft:1.21"
              mappings "net.fabricmc:yarn:1.21+build.2:v2"
              modImplementation "net.fabricmc:fabric-loader:0.15.11"
              modImplementation "meteordevelopment:meteor-client:0.5.8-SNAPSHOT"
          }
          EOF

          mkdir -p src/main/resources
          cat << 'EOF' > src/main/resources/fabric.mod.json
          {
            "schemaVersion": 1,
            "id": "autogutta-addon",
            "version": "1.0.0",
            "name": "AutoGutta Addon",
            "entrypoints": {
              "meteor": [ "com.example.addon.AddonTemplate" ]
            },
            "depends": {
              "fabricloader": ">=0.15.11",
              "minecraft": "~1.21",
              "meteor-client": "*"
            }
          }
          EOF

      - name: Build Mod with Gradle
        run: chmod +x gradlew && ./gradlew build --no-daemon

      - name: Upload Finished Jar
        uses: actions/upload-artifact@v4
        with:
          name: AutoGutta-Mod
          path: build/libs/*.jar
